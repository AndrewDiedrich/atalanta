import { mount, shallow } from 'enzyme';
// import Email from '../email';

describe('email page', () => {
    test.todo('should redirect to /login if theres no email in url params');
    // it('should redirect if no email param in url', () => {
    //     const subject = mount(<Email />);
    //     expect(subject).toHaveLength(1);

    // });
});

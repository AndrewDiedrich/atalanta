import { mount, shallow } from 'enzyme';
// import DepositButton from '../pages/depositButton';

// describe('deposit button index', () => {
//   it('should have App component', () => {
//     const subject = mount(<DepositButton />);
//     expect(subject).toHaveLength(1);
//   });
// });

// describe('deposit button details', () => {
//   const depositButton = shallow(<DepositButton />);

//   it('should be a button', () => {
//     const button = depositButton.find('button');
//     expect(button).toHaveLength(1);
//   });

//   it('should have a field to enter a quantity', () => {
//     const quantityField = depositButton.find('#deposit_quantity');
//     expect(quantityField).toHaveLength(1);
//     expect(quantityField.contains('NumericInput')).toEqual(true);
//   });
// });
